import { MatchHighlightPipe } from './match-highlight.pipe';

describe('MatchHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
